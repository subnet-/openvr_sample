#include "config.h"

/*
 *  Default values. Change them as needed.
 */

float Config::animationSpeed = 1.0f;

bool Config::localRotation = true;

glm::vec2 Config::resolution = glm::vec2(0, 0);

std::string Config::windowTitle = "New Window Title";

bool Config::submitSkybox = false;